# Known Bugs

This is basically just my current work list. 

- The TS export currently doesn't correctly import custom types.
- Zod exports should prioritize array, then default (for a default array), as the other way breaks.